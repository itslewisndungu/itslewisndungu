const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {},
		fontFamily: {
			sans: ['Inter', 'sans-serif'],
			serif: ['Labrada', 'serif']
		}
	},

	plugins: []
};

module.exports = config;
